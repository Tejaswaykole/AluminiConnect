# Project Setup & Configuration

## Prerequisites
- Node.js (for Expo Frontend)
- Python 3.10+ (for FastAPI Backend)
- PostgreSQL (Supabase)

## Global Configuration
Copy the `.env.example` file to `.env` in the project root and populate it with your Supabase and Groq keys.

## Frontend (Expo) Setup
*Phase 1 will cover the initialization of the Expo app.*

## Backend (FastAPI) Setup
*Phase 8 will cover the Python environment and pip installations.*

## Database (Supabase)
Ensure your Supabase project is created. We will handle migrations via Alembic.
