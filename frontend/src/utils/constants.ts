export const APP_CONFIG = {
  name: 'Alumni Connect',
  version: '1.0.0',
};

// Layout constants for elements that cannot use Tailwind directly (e.g. some native APIs)
export const LAYOUT = {
  basePadding: 16,
  maxContentWidth: 1200,
};
